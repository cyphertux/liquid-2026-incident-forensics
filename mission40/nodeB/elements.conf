# Mission 40 lab — identical on A and B. Only cache state may differ at runtime.
chain=regtest
server=1
listen=0
discover=0
dnsseed=0
fixedseeds=0
txindex=1
fallbackfee=0.0001
rpcuser=mission40
rpcpassword=mission40-lab-only-not-a-secret
printtoconsole=0
debug=1
debug=rpc
debug=mempool
debug=validation
# larger rangeproof cache to reduce eviction during lab
# (Elements maps validation cache sizes via -maxsigcachesize historically;
#  keep defaults unless Mission 40 build exposes knobs — documented in readiness)
